# 新種目「かたはめ（katahame）」 テスト仕様書

対象: [実装計画.md](./実装計画.md)

方針：lint＋型＋vitest 全件＋Playwright 全件をフル実行（デグレ検知）。`Category` 追加に伴う型網羅（`choiceRenderers`/`progress`/`VALID_CATEGORIES`）の解消が型レベルのデグレゲート。完了条件は全自動テスト緑。

## 自動テスト（vitest 単体）

### 幾何・レンダラー
| # | 対象 | 手順 | 期待 |
|---|---|---|---|
| U1 | `evaluateDrop` | 形一致・圏内 | `"fit"` |
| U2 | `evaluateDrop` | 形不一致・圏内 | `"miss"` |
| U3 | `evaluateDrop` | 圏外（形一致でも） | `"return"` |
| U4 | `isWithinSnap` 境界 | 距離＝半径ちょうど／半径＋ε | ちょうど=true・超過=false |
| U5 | `ShapeFitBoard` 描画 | katahame 1問を render | `data-testid="hole"`(outline・data-shape=target) と各 `piece`(data-shape/data-correct・塗り) |
| U6 | タップ設置(正解) | 正解ピースへ pointerDown→pointerUp(無移動) | `onPlace(id,true)` 1回 |
| U7 | タップ設置(誤り) | 誤ピースへ同操作 | `onPlace(id,false)` 1回 |
| U8 | ドラッグ成功 | rect スタブ・正解を穴中心へ down→move→up | `onPlace(id,true)` |
| U9 | ドラッグ誤り | 誤ピースを穴中心へ | `onPlace(id,false)` |
| U10 | 圏外ドロップ | 正解を穴から離して離す | `onPlace` 呼ばれない |
| U11 | ロック | `locked=true` で操作 | `onPlace` 発火しない |
| U12 | SSR/jsdom 安全 | rect/pointer capture 不在で render＋操作 | 例外なし・フォールバックで成立 |
| U13 | 同色描画 | 全ピース同色の問題 | 全ピース塗り色が `choice.color` で統一 |

### 型・問題データ・登録
| # | 対象 | 手順 | 期待 |
|---|---|---|---|
| U14 | `loadLesson("katahame")` | 読み込み | category=katahame／problems=12 |
| U15 | `validateLesson`(正常) | 最小正常データ | throw しない |
| U16 | `validateLesson`(target不一致) | 正解ピース形≠target | throw（target を含む） |
| U17 | `validateLesson`(ピース不正) | color欠落 or shape不正 | throw |
| U18 | `validateLesson`(同色違反) | ピース色が混在 | throw |
| U19 | `katahame.json` 整合 | 実データ全走査 | 各問 correct1・id一意・target=正解ピース形・全ピース同色・ダミー≠target・ピース2〜3・5形すべて target 登場 |
| U20 | レンダラ登録 | — | `boardRenderers.katahame===ShapeFitBoard`／`choiceRenderers` に katahame 無し／`isTapCategory("katahame")===false` |
| U21 | `progress`(katahame) | localStorage クリア→クリア記録 | クラッシュせず cleared 増・他カテゴリ不変 |
| U22 | `normalizeProgress`(後方互換) | katahame欠落の旧データ | katahame 初期値補完・既存保持 |

### QuizEngine 統合・音声整合
| # | 対象 | 手順 | 期待 |
|---|---|---|---|
| U23 | QuizEngine(完走要素) | katahame Lesson→正解ピースをタップ設置 | board 描画・星+1・「せいかい！」・演出後に次問 |
| U24 | QuizEngine(誤り) | 誤ピースをタップ設置 | ×/ふせいかい非表示・「もういちど！」・星増えず・同問継続 |
| U25 | QuizEngine(音声再利用) | katahame-009(audio→004) を render | `playClip("/audio/q/katahame-004.mp3",…)`（自問IDでない） |
| U26 | QuizEngine(既存デグレ) | color/shape Lesson を render | 従来どおり per-choice 描画（board 分岐が非干渉） |
| U27 | `audio-reuse.test`(拡張) | katahame.json＋MP3コミット後・横断走査 | katahame 生成側5・再利用側7・参照先同カテゴリ・say一致・MP3 5枚実在・孤立ゼロ・数一致 |
| U28 | 出題網羅 | 全問走査 | 5形すべてが target に最低1回（balance 4:4:4 の代替） |

## 結合（flow / e2e）— 追加機能 × 既存機能
| # | 関連 | 手順 | 期待 |
|---|---|---|---|
| I1 | katahame × home ルーティング | `tile-katahame` クリック(flow) | `push("/game/katahame")` |
| I2 | katahame × home グリッド | おうち表示(e2e) | 7タイル全可視・katahame クリックで board 到達 |
| I3 | katahame × 進行 × progress | GamePage(katahame) を設置で完走 | reward 表示・katahame.cleared=1・sticker付与・**他カテゴリ cleared 不変** |
| I4 | katahame × reward 導線 | again／home | again で星0・board 再描画／home で `push("/home")` |
| I5 | katahame × ドラッグ経路 | e2e で正解ピースを穴へドラッグ(down→move→up) | 星1点灯・せいかい演出（board→onPlace で既存フロー駆動） |
| I6 | katahame × ゴール判定 | reward まで各問設置 | 既存と同一ゴール（reward-again/home）到達 |
| I7 | katahame × 既存6種目デグレ | 既存スモーク全件 | 全緑（分岐・型変更が非干渉） |
| I8 | katahame × 音声途切れ防止/ロック | 設置→ほめ言葉完了前は進まない・演出中ロックで再設置無効 | 既存 afterFeedback/`data-locked` 挙動を board でも共有 |

## 手動テスト（人間・完了ゲート外）
| # | 対象 | 手順 | 期待 |
|---|---|---|---|
| M1 | 実機タッチ感触・スナップ半径・7タイル | 各形の穴へドラッグ | 指でも吸い付き成功しやすい・戻しがやさしい・home 崩れない |
| M2 | 穴/ピースの視認性 | 同色ピースで判別 | 色でなく形で選べる・穴が差し込み口と分かる |
| M3 | 音声「◯◯を はめてね」 | 各形・共有音声の連続問題 | 5形の読み正しい・再利用でも無音にならず連続再生 |
| M4 | reduced-motion | OS で動き低減ON | アニメ無効でも設置・ノーフェイル成立 |
| M5 | スクロール抑止 | iOS Safari/Android Chrome でドラッグ | `touch-action:none` で画面が動かず快適 |

## 受け入れ基準
- lint／typecheck／vitest 全件／e2e 全件が緑（既存デグレなし）。
- U19/U27（データ整合・音声整合）と I3/I7（progress独立・既存非干渉）が緑。
- 手動 M1〜M5 は実機確認項目として報告に明記。
