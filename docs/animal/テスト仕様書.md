# テスト仕様書: どうぶつ（animal）種目

lint + 型 + vitest 全件 + Playwright 全件をフル実行する前提（デグレ検知）。`Category` 追加に伴う型エラー解消が型レベルのデグレゲート。

## 自動テスト（tester 担当・完了ゲート）

### 単体（vitest）
| # | 対象 | 期待 |
|---|---|---|
| U1 | `loadLesson("animal")` | `category==="animal"` / `problems.length===12` |
| U2 | `validateLesson`（animal 正常） | throw しない |
| U3 | `validateLesson`（image 欠落） | throw する |
| U4 | animal.json 整合 | 各問 correct がちょうど1つ / id 一意 / 全 choice に非空 image |
| U5 | `choiceRenderers.animal` | `=== AnimalChoice` |
| U6 | `AnimalChoice` 描画 | `getByRole("button",{name:動物名})` 存在 / `<img>` の src が choice.image / img は alt="" |
| U7 | `AnimalChoice` 操作・演出 | click で onSelect 1回 / state=right・wrong で data-state 変化 |
| U8 | `QuizEngine`（animal） | 設問描画・正解で星+1・演出後に次問へ |
| U9 | progress（animal） | `recordLessonClear("animal",n)` でクラッシュせず categories.animal.cleared 増加 |

### 結合（flow.test.tsx / e2e）
| # | 検証 | 期待 |
|---|---|---|
| I1 | home `tile-animal` クリック | `/game/animal` へ遷移 |
| I2 | animal 全問正解 → clearLesson | reward 表示・categories.animal.cleared===1・stickers 増加 |
| I3 | ごほうび導線 | reward-again 再出題 / reward-home で /home |
| I4 | E2E 完走（認証バイパス） | おうち→どうぶつ→正解連打→ごほうび到達 |
| I5 | 既存 color/shape/number デグレ非発生 | 既存 vitest・e2e 全件パス |

## 手動テスト（人間・完了ゲート外）
| # | 対象 | 期待 |
|---|---|---|
| M1 | 実機タップ・4タイルレイアウト | 画像選択肢が大きく押しやすい / home が崩れない |
| M2 | 各端末で画像表示 | イラストが正しく表示される |
| M3 | 音声フォールバック | MP3 未生成でも Web Speech で say を読み上げる |
| M4 | 後日 Pro 音声差し替え | gen-audio 実行後に MP3 が優先再生される |
