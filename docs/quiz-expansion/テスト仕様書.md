# テスト仕様書: すうじ1〜10 ＋ 新種目 size/count ＋ 全6種目 4:4:4 化

lint + 型 + Vitest 全件 + Playwright 全件をフル実行する前提（デグレ検知）。`Category` 追加に伴う型エラー解消（`lessonSources`/`choiceRenderers`/`createInitialProgress`）が型レベルのデグレゲート。**既存分を含め自動テストはフル実行**する。

## 自動テスト（tester 担当・完了ゲートに含む）

### 単体（Vitest）

#### validateLesson 検証（`lib/__tests__/problems.size.test.ts` / `problems.count.test.ts`）
| # | 区分 | 対象 | 前提 | 手順 | 期待結果 |
|---|------|------|------|------|----------|
| U1 | 単体 | `loadLesson("size")` | size.json 実在 | ロード | `category==="size"` / `problems.length===12` |
| U2 | 単体 | `validateLesson`（size 正常） | 最小正常 size レッスン | 検証 | throw しない |
| U3 | 単体 | `validateLesson`（size 異常） | `size` を `"huge"` 等に改変 | 検証 | throw（メッセージに `size`） |
| U4 | 単体 | `validateLesson`（size 欠落） | `size` プロパティ削除 | 検証 | throw |
| U5 | 単体 | size.json 整合 | 実データ | 全問走査 | 各問 correct ちょうど1つ / id 一意 / 各 choice の size ∈ {large,medium,small} / 各問に large・medium・small が1つずつ |
| U6 | 単体 | `loadLesson("count")` | count.json 実在 | ロード | `category==="count"` / `problems.length===12` |
| U7 | 単体 | `validateLesson`（count 正常） | 最小正常 count レッスン | 検証 | throw しない |
| U8 | 単体 | `validateLesson`（image 欠落/空） | `image` 削除 or `""` | 検証 | throw（メッセージに `image`） |
| U9 | 単体 | `validateLesson`（count 型不正） | `count` を string 化 | 検証 | throw（メッセージに `count`） |
| U10 | 単体 | count.json 整合 | 実データ | 全問走査 | correct 1つ / id 一意 / image 非空 / count は number / 各問 3果物・3個数すべて相異 |
| U11 | 単体 | count 画像参照の網羅 | 実データ＋public | 参照 `image` 集合を収集 | §C-2 の 18 枚集合と完全一致（未使用画像・欠落なし） |

#### 全6種目 正解位置 4:4:4 検証（`lib/__tests__/problems.balance.test.ts`・横断／最重要）
| # | 区分 | 対象 | 前提 | 手順 | 期待結果 |
|---|------|------|------|------|----------|
| U12 | 単体 | color/shape/number/animal/size/count | 各 JSON=12問（shape も 12） | 各カテゴリを `loadLesson` → 各問の `correct:true` の index を集計 | index 0/1/2 それぞれちょうど 4 問（`length%3===0` かつ各位置=length/3） |
| U13 | 単体 | number 数値網羅 | number.json | 正解 `value` を収集 | 1〜10 をすべて含む（10 種の集合を包含） |
| U14 | 単体 | size 設問方向の混在 | size.json | prompt.text に「おおきい」/「ちいさい」を計数 | おおきい 6・ちいさい 6 |
| U15 | 単体 | count 設問方向の混在 | count.json | prompt.text に「おおい」/「すくない」を計数 | おおい 6・すくない 6 |

#### レンダラ（`components/quiz/renderers/__tests__/renderers.test.tsx` に追記）
| # | 区分 | 対象 | 前提 | 手順 | 期待結果 |
|---|------|------|------|------|----------|
| U16 | 単体 | `SizeChoice` 描画 | large の SizeChoice | render | `getByRole("button",{name:"おおきい"})` 存在 / 内部に図形 SVG / ラッパに `sizeLarge` クラス相当 |
| U17 | 単体 | `SizeChoice` サイズ分岐 | medium/small | render | それぞれ `sizeMedium`/`sizeSmall` 相当のラッパで描画 |
| U18 | 単体 | `SizeChoice` 操作・演出 | onSelect spy | click / state 切替 | click で onSelect 1回 / `data-state` が right・wrong に変化 / `data-correct` 反映 |
| U19 | 単体 | `CountChoice` 描画 | image 付き CountChoice | render | `getByRole("button",{name:"いちご いつつ"})` / `<img>` の src が choice.image / img は `alt=""` |
| U20 | 単体 | `CountChoice` 操作・演出 | onSelect spy | click / state 切替 | click で onSelect 1回 / `data-state` 変化 / `data-correct` 反映 |
| U21 | 単体 | `choiceRenderers` | index.ts | 参照 | `.size===SizeChoice` かつ `.count===CountChoice`（既存4種の対応も維持） |

#### progress 後方互換（`lib/__tests__/progress.test.ts` に追記）
| # | 区分 | 対象 | 前提 | 手順 | 期待結果 |
|---|------|------|------|------|----------|
| U22 | 単体 | `recordLessonClear("size",n)` / `("count",n)` | ― | 実行 | クラッシュせず `categories.size.cleared`/`.count.cleared` が加算 / 他カテゴリ不変 |
| U23 | 単体 | `normalizeProgress`（size/count キー欠落） | size/count を含まない旧フォーマット保存 | `loadProgress` | 欠けた size/count が `{cleared:0,lastStars:0}` で補完 / 既存値保持（**ループ配列への追記漏れ検知**） |

### 結合（`app/(kid)/__tests__/flow.test.tsx` / `e2e/smoke.spec.ts`）

追加機能 × 既存機能の関連を突く（共有状態=progress、画面遷移=home→game→reward、レンダラ委譲、pickProblems 抽出、ノーフェイル進行）。

| # | 区分 | 追加機能 × 既存機能の関連 | 前提 | 手順 | 期待結果 |
|---|------|---------------------------|------|------|----------|
| I1 | 結合(flow) | home タイル × ルーティング | HomePage | `tile-size` クリック | `push("/game/size")` |
| I2 | 結合(flow) | home タイル × ルーティング | HomePage | `tile-count` クリック | `push("/game/count")` |
| I3 | 結合(flow) | size 種目 × QuizEngine/progress 共有状態 | category=size | 完走（正解連打） | reward 表示 / `categories.size.cleared===1` / `color.cleared===0`（独立性）/ stickers 増加 |
| I4 | 結合(flow) | count 種目 × 画像レンダラ委譲/progress | category=count | 完走 | reward 表示 / `categories.count.cleared===1` / 選択肢に `<img>` 存在 / stickers 増加 |
| I5 | 結合(flow) | ごほうび導線 × 再出題（size/count） | 完走後 | reward-again / reward-home | again で星0再開・choice 再描画 / home で `push("/home")` |
| I6 | 結合(flow) | 既存 color/shape/number/animal デグレ非発生 | 4:4:4 化・1〜10 化後 | 既存 flow スイート全実行 | 全件パス（先頭3問維持・完走・進捗加算が不変） |
| I7 | 結合(e2e) | size 種目巡回（認証バイパス） | dev+E2E認証 | おうち→`tile-size`→正解連打 | reward 到達（「よく できました！」/ reward-again/home 可視）。図形 SVG が描画される |
| I8 | 結合(e2e) | count 種目巡回 | 同上 | おうち→`tile-count`→正解連打 | 最初の出題で `[data-testid="choice"] img` 可視 → reward 到達 |
| I9 | 結合(e2e) | 既存4種目デグレ非発生 | ― | 既存 color/shape/number/animal spec 全実行 | 全件パス |
| I10 | 結合(e2e) | home 6 タイル共存 | おうち | 6 タイルが全て表示・各 testid 可視 | tile-color/shape/number/animal/size/count がすべて存在しクリック可能 |

## 手動テスト（人間・完了ゲート外・記載のみ）

| # | 区分 | 対象 | 前提 | 手順 | 期待結果 |
|---|------|------|------|------|----------|
| M1 | 手動 | おおきさの見た目 | 実機/実ブラウザ | size を数レッスンプレイ | 大中小が明確に視認でき、正解が判断できる。おおきい/ちいさい設問が両方出る |
| M2 | 手動 | かずの画像 | 各端末 | count をプレイ | お皿＋果物 n 個が正しく（個数一致・重なりなし）表示。多い/少ない設問が両方出る |
| M3 | 手動 | 正解位置の散らばり | 実機 | 全6種目を複数レッスン反復 | 正解が左・中・右に偏らず散る（体感 4:4:4） |
| M4 | 手動 | 音声読み上げ（新設問） | 実機 | size/count/新 number 設問を出題・マスコットタップ | MP3 未生成でも Web Speech で say を読み上げ。生成後は MP3 が優先再生 |
| M5 | 手動 | タップ挙動 | 実機（スマホ/タブレット） | 各種目で選択肢タップ | タップ領域が十分・押しやすい。誤答でもフェイル表示なく同問に留まる |
| M6 | 手動 | home 6 タイルレイアウト | 各画面幅 | おうち表示 | 6 タイルが折り返して収まり縦にはみ出さない。色（size=橙/count=黄）が区別できる |
| M7 | 手動 | 後日 Pro 音声差し替え | クォータ回復後 | `gen:audio:gemini:prod` 実行 | 新設問 MP3 が生成され自動で優先再生に切替 |
